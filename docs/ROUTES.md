# Routes

- `/` – Landing page
- `/plan` – Itinerary planner
- `/transport` – Transport search (train/bus/tuk)
- `/discover` – Places & experiences
- `/map` – Interactive map

Each route should:
- Render header + footer (from layout)
- Handle loading/empty/error states
- Use design-system components
